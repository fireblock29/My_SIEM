filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/snort/*.log
    - /var/log/snort/*/*.log

output.elasticsearch:
  hosts: ['http://elasticsearch:9200']
setup.kibana:
  host: "kibana:5601"
